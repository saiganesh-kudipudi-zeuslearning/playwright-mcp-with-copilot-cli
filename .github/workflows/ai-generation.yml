name: AI Playwright Generation

on:
  workflow_dispatch:
    inputs:
      prompt:
        description: "Custom prompt (leave blank to use prompts/build-todo.txt)"
        required: false
        default: ""

permissions:
  contents: write
  pull-requests: write

jobs:
  generate:
    runs-on: ubuntu-latest

    env:
      COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Project Dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium

      - name: Install Copilot CLI
        run: npm install -g @github/copilot

      - name: Verify Copilot CLI
        run: copilot --version

      - name: Verify GitHub CLI
        run: gh --version

      - name: Configure Git Identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Resolve Prompt
        run: |
          ./.github/scripts/resolve-prompt.sh "${{ inputs.prompt }}"

      - name: Generate Feature Branch
        run: |
          ./.github/scripts/generate-branch-name.sh

      - name: Run AI Generation
        env:
          PROMPT: ${{ env.PROMPT }}
        run: |
          ./.github/scripts/run-ai-generation.sh

      - name: Generate PR Title
        run: |
          ./.github/scripts/generate-pr-title.sh

      - name: Create Pull Request
        env:
          GH_TOKEN: ${{ secrets.GH_CLASSIC_TOKEN }}
        run: |
          ./.github/scripts/create-pr.sh

      - name: Upload OTel Traces
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: copilot-traces
          path: copilot-traces.jsonl

      - name: Upload Copilot Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: copilot-logs
          path: ~/copilot-logs/